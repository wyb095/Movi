# Safe defaults used when local.properties does not define a Maps key.
# This keeps the project buildable, but map search/view stay disabled until
# a shared team key is added to local.properties.
MAPS_API_KEY=DISABLED
